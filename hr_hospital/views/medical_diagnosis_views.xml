<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_medical_diagnosis_tree" model="ir.ui.view">
        <field name="name">medical.diagnosis.tree</field>
        <field name="model">medical.diagnosis</field>
        <field name="arch" type="xml">
            <tree>
                <field name="visit_id"/>
                <field name="disease_id"/>
                <field name="severity"/>
                <field name="is_approved"/>
                <field name="approved_by_id"/>
            </tree>
        </field>
    </record>

    <record id="view_medical_diagnosis_form" model="ir.ui.view">
        <field name="name">medical.diagnosis.form</field>
        <field name="model">medical.diagnosis</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_approve"
                            string="Approve"
                            type="object"
                            class="oe_highlight"
                            invisible="is_approved"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="visit_id"
                                   domain="[('state', '=', 'completed'),
                                            ('scheduled_datetime', '&gt;=',
                                             (context_today() - relativedelta(days=30)).strftime('%Y-%m-%d'))]"/>
                            <field name="disease_id"/>
                            <field name="severity"/>
                        </group>
                        <group>
                            <field name="is_approved"/>
                            <field name="approved_by_id"/>
                            <field name="approval_date"/>
                        </group>
                    </group>
                    <group string="Description">
                        <field name="description" nolabel="1"/>
                    </group>
                    <group string="Treatment">
                        <field name="treatment" nolabel="1"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_medical_diagnosis_search" model="ir.ui.view">
        <field name="name">medical.diagnosis.search</field>
        <field name="model">medical.diagnosis</field>
        <field name="arch" type="xml">
            <search>
                <field name="disease_id"/>
                <field name="visit_id"/>
                <filter string="Approved"
                        name="approved"
                        domain="[('is_approved', '=', True)]"/>
                <filter string="Not Approved"
                        name="not_approved"
                        domain="[('is_approved', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Disease"
                            name="group_disease"
                            context="{'group_by': 'disease_id'}"/>
                    <filter string="Severity"
                            name="group_severity"
                            context="{'group_by': 'severity'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Pivot View -->
    <record id="view_medical_diagnosis_pivot" model="ir.ui.view">
        <field name="name">medical.diagnosis.pivot</field>
        <field name="model">medical.diagnosis</field>
        <field name="arch" type="xml">
            <pivot string="Diagnosis Analysis">
                <field name="disease_id" type="row"/>
                <field name="disease_type" type="row"/>
                <field name="diagnosis_date" interval="month" type="col"/>
            </pivot>
        </field>
    </record>

    <!-- Graph View -->
    <record id="view_medical_diagnosis_graph" model="ir.ui.view">
        <field name="name">medical.diagnosis.graph</field>
        <field name="model">medical.diagnosis</field>
        <field name="arch" type="xml">
            <graph string="Disease Statistics" type="bar">
                <field name="disease_type"/>
                <field name="diagnosis_date" interval="month" type="col"/>
            </graph>
        </field>
    </record>

    <record id="action_medical_diagnosis_window" model="ir.actions.act_window">
        <field name="name">Diagnoses</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">medical.diagnosis</field>
        <field name="view_mode">tree,form,pivot,graph</field>
    </record>
</odoo>
